# RUST_TASK_TEMPLATE (copy/paste into your IDE agent)

You are working in a Rust repository governed by `.agent/rules/`.

## Task
<describe the task>

## Constraints
- No `unsafe` unless explicitly required.
- No new dependencies unless strictly necessary; if added, run supply-chain checks and justify.
- No fabricated tool output.

## Required deliverables
1) Plan (3–7 steps, minimal).
2) Diff (show changed files).
3) Commands executed (verbatim).
4) Results summary from tools.
5) Checklist (pass/fail).
6) If blocked, explain exactly why and what command the user must run.

## Definition of DONE
`./tools/verify.sh` is GREEN.
